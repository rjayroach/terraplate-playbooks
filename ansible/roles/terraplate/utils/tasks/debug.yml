---
- name: 'Terraplate debug as seen in {{ called_from }}'
  debug:
    var: '{{ debug_item }}'
    verbosity: 1
  with_items: [manifest, spec_a, spec_b, spec, resource, user_resource, user_key]
  loop_control:
    loop_var: debug_item
